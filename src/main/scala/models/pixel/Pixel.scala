package models.pixel

/**
 * A trait that represents a pixel.
 *
 * This trait can be extended by any class that needs to represent a pixel.
 * Specific pixel types (e.g., grayscale, RGB, Ascii, etc.) should provide their own
 * implementation of the methods and properties defined in this trait.
 */
trait Pixel {

}

