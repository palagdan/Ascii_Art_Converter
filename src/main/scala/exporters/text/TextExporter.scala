package exporters.text
import exporters.Exporter


/**
 * A trait that extends the Exporter trait to export text.
 * @tparam String the type of data that the exporter will export.
 */
trait TextExporter extends Exporter[String]{

}

