package filters.image.grayFilters.specific.flip

import filters.image.grayFilters.GrayImageFilter

/**
 * Trait `FlipFilter` represents a generic interface for flipping grayscale images.
 *
 * This trait extends the `GrayImageFilter` trait.
 */
trait FlipFilter extends GrayImageFilter {

}

